-- in --
<component name="main" params-as="params">
	<for each="params" as="j">
		<cond if="j.a">
		</cond>
	</for>
	<for each="params" as="j">
		<cond if="j.b">
		</cond>
	</for>
</component>
-- out --
main : {a: boolean, b: boolean}[]
