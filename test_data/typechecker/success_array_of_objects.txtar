-- main.hop --
<component name="main" params-as="params">
	<for each="params.items" as="item">
		<cond if="item.active">
		</cond>
		<cond if="item.name">
		</cond>
	</for>
</component>
-- out --
main : {items: {active: boolean, name: boolean}[]}
